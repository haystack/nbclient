const CommentVisibility = Object.freeze({
  EVERYONE: 0,
  INSTRUCTORS: 1,
  MYSELF: 2
})

const CommentAnonymity = Object.freeze({
  IDENTIFIED: 0,
  ANONYMOUS: 1
})

export {
  CommentVisibility,
  CommentAnonymity
}
